# Backend API base URL used by the onboarding signup form
# (POST {VITE_API_URL}/api/auth/signup)
# Leave empty if your web app is served behind the same origin as the API.
VITE_API_URL=https://api.playbyplayai.org
